# Frontend API base URL.
# Local dev can keep this empty to use Vite proxy (/api -> localhost:8000).
# Production should point to your deployed backend, e.g.:
# VITE_API_BASE_URL=https://your-backend-domain.com/api
VITE_API_BASE_URL=
